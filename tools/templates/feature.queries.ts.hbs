export const make{{featureClassName}}Queries = () => ({
  getItems: async (): Promise<string> => {
    return Promise.resolve('Hello world');
  }  
})

export type {{featureClassName}}Queries = ReturnType<typeof make{{featureClassName}}Queries>;
